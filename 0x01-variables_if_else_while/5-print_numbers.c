#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int d = 0;

	for (; d != 10; d++)
		printf("%d", d);
	printf("\n");
	return (0);
}
